## What changed

Audit Trail passed its retention and export check. The Partner API sample still exposed an internal note from the older serializer. The EU Region production-size load test missed the search-response limit.

## What was decided

The Partner API first release uses read-only scopes. Security needs a clean restricted-account sample before approving partner credentials.

## What is unresolved

The Partner API review remains blocked. EU Region needs another production-size run after capacity and query-plan changes.
