'use client';

import { HistorySidebar } from './history-sidebar';

/** Conversations live in the app sidebar, the same rail as New chat and Dreams. */
export function ChatHistoryNav() {
  return <HistorySidebar />;
}
